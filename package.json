{
  "name": "genlayer-project",
  "type": "module",
  "private": true,
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "test": "cd frontend && npm test",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "lint": "cd frontend && npm run lint",
    "test:fees": "gltest tests/integration/test_uphold_fee_profile.py --network localnet --chain-type localnet --fee-profile artifacts/uphold-fee-profile.ci.json --fee-profile-headroom 1.25 -v -s",
    "deploy": "genlayer deploy"
  },
  "devDependencies": {
    "genlayer": "0.40.0-rc.3",
    "genlayer-js": "2.0.0-rc.1"
  }
}
